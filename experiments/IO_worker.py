import os, json, sys

class file:
    CWD = os.getcwd()

    @staticmethod
    def path_fiddler(input): # makes sure that the file is either a json file or a string, if not the case: raise error    
        CWD = os.getcwd()

    @staticmethod
    def load(path):#
        pass